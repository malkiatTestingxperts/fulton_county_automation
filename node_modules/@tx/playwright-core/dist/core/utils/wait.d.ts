import type { Page } from "@playwright/test";
/**
 * Waits until the element is visible.
 */
export declare function waitForElementToVisible(page: Page, selector: string, label: string): Promise<void>;
