import { Page, Locator } from "@playwright/test";
/**
 * Tries multiple locators manually first; if all fail, falls back to AI self-healing.
 */
export declare function findElementUniversal(page: Page, locators: string[], elementName?: string, timeout?: number): Promise<Locator>;
