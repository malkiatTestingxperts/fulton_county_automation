import type { Page } from "@playwright/test";

export async function launchURL(page: Page, baseURL: string): Promise<void> {
    // Go to login page
    await page.goto(baseURL, { waitUntil: 'domcontentloaded', timeout: 120000 });
}