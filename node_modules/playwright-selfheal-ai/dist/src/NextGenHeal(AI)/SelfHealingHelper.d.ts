import { Page } from "@playwright/test";
import { LocatorStore } from "./LocatorStore";
export declare class SelfHealingHelper {
    private page;
    private store;
    private store;
    constructor(page: Page, store: LocatorStore);
    find(selector: string): Promise<import("playwright-core").Locator>;
    private filterValidCandidates;
    DateTime(date: Date): string;
}
