import { Page } from "playwright";
import { LogLevel } from "./Logger";
export interface SelfHealingConfig {
    logLevel?: LogLevel;
}
export interface SelfHealingConfig {
    enableLogs?: boolean;
}
export declare class SelfHealingPage {
    private page;
    private config;
    private helper;
    private store;
    constructor(page: Page, config?: SelfHealingConfig);
    /**
     * Returns a proxy Locator wrapper.
     * Any Playwright Locator action (click, fill, type, press, hover, check, selectOption, etc.)
     * will automatically run through self-healing logic.
     */
    locator(selector: string): {};
}
