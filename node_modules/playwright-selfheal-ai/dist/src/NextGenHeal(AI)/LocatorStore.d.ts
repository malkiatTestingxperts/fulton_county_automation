import { Candidate } from "./HealingStrategies";
export interface HealingEntry {
    original: string;
    healed: string | null;
    candidates: Candidate[];
    meta: Record<string, any>;
    updatedAt: string;
}
export declare class LocatorStore {
    private store;
    constructor();
    get(selector: string): HealingEntry | undefined;
    save(original: string, healed: string | null, candidates: Candidate[], meta?: Record<string, any>): void;
    update(stableKey: string, original: string, healed: string | null, candidates: Candidate[], meta?: Record<string, any>): void;
    list(): Record<string, HealingEntry>;
    /**
     * Remove entries not updated in last X days
     */
    cleanupStale(days?: number): void;
    private persist;
}
