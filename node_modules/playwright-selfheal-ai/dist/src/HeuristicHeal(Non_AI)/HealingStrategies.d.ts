import { Page } from "@playwright/test";
export interface Candidate {
    type: string;
    selector: string;
    score?: number;
    updatedAt?: string;
}
export interface HealingConfig {
    enableLogs?: boolean;
}
export declare function configureHealing(config: HealingConfig): void;
export declare function heuristicScore(candidate: Candidate, meta: any): number;
export declare function generateLocatorCandidates(page: Page, originalSelector: string): Promise<Candidate[]>;
